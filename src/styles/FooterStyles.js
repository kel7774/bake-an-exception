import styled from 'styled-components';

const FooterStyles=styled.footer`
    width: 100%;
    position: fixed;
    bottom: 0;
        .content {
            display: flex;
            flex-direction: row;
        }
`;

export default FooterStyles;